export declare type SizedIconButtonStyleProps = {
    padding: string | number;
    color: string;
    childSize: string | number;
};
declare const _default: () => {
    root: ({ padding, color }: SizedIconButtonStyleProps) => {
        color: string;
        padding: string | number;
    };
    label: ({ childSize }: {
        childSize?: number;
    }) => {
        '& > *': {
            fontSize: number;
        };
    };
};
export default _default;
